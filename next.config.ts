import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true, // Adicione esta linha para ativar o modo estrito do React
  /* config options here */
};

export default nextConfig;
